{
  "environments": [
    {
      "name": "prd",
      "configOverride": {
        "defaultSchema": "dataform",
        "defaultDatabase": "gglobo-geoip-hdg-prd",
        "vars": {
          "env": "prd"
        }
      },
      "gitRef": "master"
    },
    {
      "name": "dev",
      "configOverride": {
        "defaultSchema": "dataform",
        "defaultDatabase": "gglobo-geoip-hdg-dev",
        "vars": {
          "env": "dev"
        }
      },
      "gitRef": "dev"
    }
  ]
}