config {
  tags: ['staging'],
  dependencies: [{schema: 'staging', name: 'city_blocks_ipv4'}],
  name: "update_staging_dataset"
}

ALTER SCHEMA staging
 SET OPTIONS (
     description = 'Dataset que contém as tabelas intermediárias, com campos de ip inicial e final',
     default_table_expiration_days = 30,
      labels = [
      ('createdby', 'dataform'),
      ('team', 'dataproducts')
    ]
);
